#include <stdio.h>
int razlicni_broevi(int x, int broj)
{
    int tmp, f = 1,fix, counter = 0;
    f = broj;
    while(f != 0)
    {
        tmp =  broj;

        fix = broj%10;
        while(tmp != 0)
     {
        if((tmp%100)/10 != fix)     //1234
        {
            counter++;
        }
        tmp /= 10;
     }
      f = f /10;

    }
    if(counter != x)
        return 0;
}
int main()
{
    int x,n,array[100];
    scanf("%d", &x);
    scanf("%d", &n);
    for(int i = 0; i < n; i++)
    {
        scanf("%d", &array[i]);
    }
     for(int i = 0; i < n; i++)
    {
        if(!(razlicni_broevi(x,array[i])))
        {
            printf("%d\n",array[i]);
        }
    }
    return 0;
}
